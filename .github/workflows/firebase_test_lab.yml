# Updated Firebase Test Lab Configuration

# ... (other configurations)

    --device model=oriole,version=33,locale=en,orientation=portrait \
    --device model=a51,version=32,locale=en,orientation=portrait \

# ... (other configurations)